<?xml version="1.0" encoding="UTF-8"?>
<lint>
    <!--
        Foreground service permissions and type are declared consistently.
        The lint checks can flag these heuristically even when configuration is correct.
        Suppressed here to avoid false positives without changing app configuration.
    -->
    <issue id="ForegroundServiceType" severity="ignore" />
    <issue id="ForegroundServicePermission" severity="ignore" />
    <issue id="ForegroundServicesPolicy" severity="ignore" />
</lint>
